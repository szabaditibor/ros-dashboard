import { Box, Text } from "@chakra-ui/react";

const FallbackInstrument = () => {
  return (
    <Box>
      <Text>This is a fallback instrument</Text>
    </Box>
  );
};

export default FallbackInstrument;
